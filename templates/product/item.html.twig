{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}
{% endblock %}

{% block body %}
    <div class="container g-3 mt-3 col-4 justify-content-start">
        <h1 class="">{{ product.name}}</h1>
        <h3>{{ product.category.name }}</h3>
        <h5>{{ product.baseprice }}€</h5>
        <p>{{ product.description }}</p>
    </div>
{% endblock %}